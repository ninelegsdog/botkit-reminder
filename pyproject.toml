[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "botkit-reminder"
version = "0.2.0"
description = "Telegram bot for reminders and scheduled messages"
requires-python = ">=3.12"
dependencies = [
    "aiogram>=3.30",
    "apscheduler>=3.10",
    "fastapi>=0.100",
    "httpx>=0.27",
    "pydantic>=2.0",
    "pydantic-settings>=2.0",
    "redis>=5.0",
    "sqlalchemy[asyncio]>=2.0",
    "aiosqlite>=0.19",
    "asyncpg>=0.29",
    "botkit-core @ git+https://github.com/ninelegsdog/botkit-core@v0.5.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8",
    "pytest-asyncio>=0.23",
    "pytest-cov>=5",
    "pytest-xdist>=3.6",
    "hypothesis>=6.0",
    "ruff>=0.5",
    "mypy>=1.10",
    "fastapi>=0.100",
    "httpx>=0.27",
    "aiohttp>=3.9",
    "requests>=2.31",
    "structlog>=24.0",
]
e2e = [
    "telethon>=1.34",
    "python-dotenv>=1.0",
]

[project.scripts]
bot = "botkit_reminder.bot:main"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
addopts = "-ra --strict-markers --cov=src --cov-branch --cov-report=term-missing --cov-fail-under=70"
filterwarnings = [
    "ignore::DeprecationWarning:starlette.testclient",
    "error::DeprecationWarning",
    "ignore::DeprecationWarning:The anyio.abc.BlockingPortal alias is deprecated",
    "ignore::DeprecationWarning:anyio.abc.BlockingPortal",
]
markers = [
    "integration_test: marks tests as integration tests (deselect with -m not integration_test)",
    "no_req: fully offline, no external HTTP/Telegram requests",
    "req: requires network and/or a real external service (Telegram E2E, payment gateways)",
    "unit: isolated unit test",
    "webhook: HTTP/webhook endpoint test",
    "e2e: real Telegram E2E via MTProto",
    "serial: must not run in parallel via xdist",
]

[tool.coverage.run]
branch = true
source = ["src"]
parallel = true
omit = ["tests/*", "scripts/*", "bot.py", "src/core/tgwebhook.py"]

[tool.ruff]
line-length = 120
ignore = ["BLE001", "E711", "S110", "LOG015", "PYI036", "DTZ007", "TRY401"]

[tool.mypy]
python_version = "3.12"
strict = true
ignore_missing_imports = true
warn_unused_ignores = false
disable_error_code = ["attr-defined", "no-any-return", "call-arg", "arg-type"]
